using System;
using UnityEngine;

namespace ws.winx.input
{

    /*
     * 
     * InputProcessor
     * 
     * 
     * Default functional keys designator
     * & = Alternate, ^ = Control, % = Command/Windows key, # = Shift 
           Examples: &f12 = Alternate + F12, "^[0]" = Control + keypad0 .
     * */
    static class InputProcessor
    {
       
         /* num of combinations allowed default=4 ex(w+fire0(x2)+fire1+a) */
        //public static int NUM_COMBINATIONS = 4;
       
        
       


        



            
        
			}  
}
